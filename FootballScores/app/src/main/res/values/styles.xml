<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
    </style>

    <!-- Main activity theme. -->
    <style name="FootballScoresTheme" parent="AppTheme">
        <item name="actionBarStyle">@style/ActionBar.Solid.FootballScores</item>
    </style>

    <!-- main activity action bar styles -->
    <style name="ActionBar.Solid.FootballScores" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="displayOptions">useLogo|showHome|showTitle</item>
        <item name="logo">@drawable/ic_launcher</item>
    </style>

</resources>
